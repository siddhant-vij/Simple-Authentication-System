name: simple_authentication_system
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.0.6

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  crypto: ^3.0.3
  csv: ^5.0.2

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
